name: CI
on:
  pull_request:
    branches: [master]
jobs: 
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: ["14", "16", "17"]
        version: ["latest"]        
    name: Test Node V${{ matrix.node }} react@${{ matrix.version }}
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}
          cache: 'yarn'      

      - name: install
        run: yarn

      - name: Set Version
        run: |
          yarn up react@${{ matrix.version }} react-test-renderer@${{ matrix.version }}          
        if: ${{ matrix.version != 'latest'}} 
      
      - name: build
        run: yarn workspace test-react-hooks build

      - name: lint
        run: yarn lint

      - name: test
        run: yarn test
          
